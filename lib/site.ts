/**
 * SITE LOGO
 * ------------------------------------------------------------------
 * Put the logo file in the  public/brand/  folder, then make sure the
 * path below matches its name EXACTLY -- including capital letters
 * and the file extension. The live site is case-sensitive, so
 * "Logo.PNG" and "logo.png" count as different files.
 *
 * Tip: crop away any empty transparent space around the logo before
 * adding it. The size below is the height of the whole image, so
 * empty padding makes the visible logo look smaller.
 * ------------------------------------------------------------------
 */
export const logoSrc = "/brand/logo.png";

/**
 * Logo height in pixels. It scales between these two sizes:
 * `logoHeightMobile` on phones, growing to `logoHeight` on wide screens.
 */
export const logoHeight = 150;
export const logoHeightMobile = 96;

/** Show the text "xmimiso" under the logo. Off, since the logo already has the name. */
export const showSiteName = false;
export const siteName = "xmimiso";
